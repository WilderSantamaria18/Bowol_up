# Workflow de despliegue continuo (Placeholder)
name: Deployment

on:
  workflow_dispatch:
  # push:
  #   tags:
  #     - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
      # - name: Deploy step placeholder
      #   run: echo "Deploy pipeline placeholder"
